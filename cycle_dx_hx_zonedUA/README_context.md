# Context

This directory is **Variant C (`cycle_dx_hx_zonedUA`)**.

It differs from baseline only in heat-exchanger modeling: evaporator is
2-zone (two-phase + superheat), condenser is 3-zone
(desuperheat + two-phase + subcool).

See: `hx_models.py`.
