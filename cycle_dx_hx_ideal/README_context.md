# Context

This directory is **Variant A (`cycle_dx_hx_ideal`)**.

It differs from baseline only in that heat-exchanger calls are routed through a
standard interface in `hx_models.py`; behavior remains idealized.

See: `hx_models.py`.
